server:
  port: 8031

spring:
  application:
    name: oj-codeSandBox-service
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        group: GROUP
  config:
    import[0]: nacos:oj-codeSandBox-service-dev.yaml?group=DEFAULT_GROUP
    import[1]: nacos:oj-common-dev.yaml?group=DEFAULT_GROUP